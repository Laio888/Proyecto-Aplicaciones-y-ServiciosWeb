@page
@model ApiKnowledgeMap.Pages.AspectoNormativo.IndexModel
@{
    ViewData["Title"] = "Aspectos Normativos";
}

<h2>Aspectos Normativos</h2>
<a href="/AspectoNormativo/Crear" class="btn btn-success mb-3">+ Nuevo</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Tipo</th>
            <th>Descripción</th>
            <th>Fuente</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Items)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Tipo</td>
                <td>@item.Descripcion</td>
                <td>@item.Fuente</td>
                <td>
                    <a href="/AspectoNormativo/Editar/@item.Id" class="btn btn-sm btn-warning">Editar</a>
                    <a href="/AspectoNormativo/Eliminar/@item.Id" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>